#pragma once

#include <tamagochi/logic/game_config.h>

/**
 * Runs the Tamagochi game with the given configuration.
 * @param config The configuration for the game.
 */
void RunGame(GameConfig *config);